% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ols.R
\name{ols_fit}
\alias{ols_fit}
\title{OLS estimation with robust or cluster-robust standard errors}
\usage{
ols_fit(Y, X, c_id = NULL, s_wgt = NULL, nocons = FALSE, silent = FALSE)
}
\arguments{
\item{Y}{Numeric vector (length n): dependent variable.}

\item{X}{n x K numeric matrix or data frame: regressors. A constant is
prepended unless \code{nocons = TRUE}.}

\item{c_id}{Optional vector (length n): cluster identifiers for
cluster-robust standard errors. If \code{NULL}, heteroscedastic-robust
(HC) standard errors are reported.}

\item{s_wgt}{Optional numeric vector (length n): sampling weights,
normalised internally to have mean one.}

\item{nocons}{Logical. If \code{TRUE}, do NOT prepend a constant column.
Set to \code{TRUE} only when \code{X} already contains a constant.}

\item{silent}{Logical. If \code{TRUE}, suppress printed output.}
}
\value{
A list with components:
  \describe{
    \item{beta}{K x 1 matrix of OLS estimates.}
    \item{vcov}{K x K variance-covariance matrix.}
    \item{hess_logl}{K x K Hessian (= X'X with weights).}
    \item{score_i}{n x K matrix of score contributions.}
    \item{ehat}{n x 1 matrix of fitted values.}
  }
}
\description{
Computes OLS coefficient estimates and returns the score matrix needed
for dyadic-robust variance calculations. Port of Graham's Python
\code{ipt.ols}.
}
